= form_for ([@trip, Member.new]) do |f|
  .form-group
    .col-lg-offset-2.col-lg-10
      = f.text_field :name
      = f.text_field :last_name
    .col-lg-offset-2.col-lg-10
      = f.label 'dodaj uczestnika'
      = f.button  :submit ,class: 'btn btn-primary'

