= simple_form_for @trip.member.expence, :html => { :class => 'form-horizontal' } do |f|
  = f.input :ammount
  = f.input :what_for
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), expences_path, :class => 'btn btn-default'
