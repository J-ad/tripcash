.page-header
  .h1
    =t '.title', :default => Trip.human_attribute_name(:trips)
    = link_to '+', new_trip_path, class: 'mdl-button mdl-js-button mdl-button--fab mdl-button--colored'
    -# do
      %i.glyphicon.glyphicon-user
      Add user
%table.table.table-striped
  %thead
    %tr
      %th= Trip.human_attribute_name(:destination)
      %th= Trip.human_attribute_name(:started_at)
      %th= Trip.human_attribute_name(:ended_at)
      %th=t '.actions', default: t("helpers.actions")
  %tbody
    -  @trips.each do |trip|
      %tr
        %td= link_to trip.destination, trip_path(trip), class: 'btn btn-default btn-xs'
        %td= trip.started_at
        %td= trip.ended_at
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_trip_path(trip), :class => 'btn btn-default btn-xs'
          = link_to t('.show', :default => t("helpers.links.show")), trip_path(trip), :class => 'btn btn-default btn-xs'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), trip_path(trip), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs btn-danger'

= link_to t('.new', :default => t("helpers.links.new")), new_trip_path, :class => 'btn btn-primary'
